var circleRadios = Number(prompt("Enter the value of a circle’s radius"));
confirm(`Total area of the circle is ${Math.PI * Math.pow(circleRadios, 2)}`);

var userNum = Number(prompt("what`s value to calculate its square root?"));
confirm(`squar root of ${userNum} is ${Math.sqrt(userNum)}`);

// var area = function (radios) {
//   return Math.PI * Math.pow(radios, 2);
// };

// var calcSqRoot = function (value) {
//   return Math.sqrt(value);
// };

// console.log(area(5));
// console.log(calcSqRoot(16));
